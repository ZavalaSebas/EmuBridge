<Window x:Class="EmuBridge.GameDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Name}" Height="640" Width="420"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="220">
            <Border Background="#333333" CornerRadius="4">
                <TextBlock Text="No Cover" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <Image Source="{Binding CoverImagePath}" Stretch="Uniform" />
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding Name}" FontSize="18" FontWeight="SemiBold"
                   TextWrapping="Wrap" Margin="0,12,0,0" />
        <TextBlock Grid.Row="2" Text="{Binding PlatformName}" Foreground="Gray" Margin="0,4,0,0" />
        <TextBlock Grid.Row="3" Text="{Binding ReleaseYearText}" Margin="0,4,0,0" />

        <!-- Bound to TheGamesDbService's outcome (metadata-source decision, 2026-08-04) — falls
             back to the same explicit "Description: not available" text ADR-19 established for
             every case where there's nothing real to show, never left hidden or blank. -->
        <ScrollViewer Grid.Row="4" Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
            <TextBlock Text="{Binding DescriptionText}" Foreground="Gray"
                       VerticalAlignment="Top" TextWrapping="Wrap" />
        </ScrollViewer>

        <ScrollViewer Grid.Row="5" Height="110" Margin="0,12,0,0"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                      Visibility="{Binding HasScreenshots, Converter={StaticResource BoolToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding ScreenshotPaths}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding}" Height="100" Margin="0,0,6,0" Stretch="Uniform" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="6" Content="Close" IsCancel="True" HorizontalAlignment="Right"
                Padding="20,6" Margin="0,12,0,0" />
    </Grid>
</Window>
