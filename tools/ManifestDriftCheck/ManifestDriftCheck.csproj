<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ManifestDriftCheck</RootNamespace>

    <!-- CI-only implementation detail of the manifest-drift-check GitHub Action (ARCHITECTURE.md
         -> ADR-25) — never invoked manually by a maintainer, never shipped in Bridge.exe. Lives
         under tools/ specifically so it never enters Bridge.csproj's publish output. -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SharpCompress" Version="0.50.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Bridge\Bridge.csproj" />
  </ItemGroup>

</Project>
